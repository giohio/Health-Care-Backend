[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = session
testpaths = tests/e2e tests/integration
log_cli = true
log_cli_level = INFO
markers =
    e2e: end-to-end tests requiring live services
    integration: cross-service tests requiring live services
